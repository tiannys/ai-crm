# Frontend API endpoint used by the browser.
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Optional: comma-separated additional hostnames allowed to access the Next.js
# development server (for example, a Cloudflare Tunnel hostname).
# Use hostnames only, without http:// or https://. Localhost is allowed by default.
ALLOWED_DEV_ORIGINS=""
